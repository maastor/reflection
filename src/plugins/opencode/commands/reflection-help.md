---
description: Quick-reference card for the reflection plugin
---
Show the reflection quick reference: the commands (/reflection, /reflection
<slug>, /reflection stop, /reflection-init, /reflection-log), the loop (pick one
question → read changelog → investigate → surface one validated issue → propose
minimal fix → apply on approval → log), the question slugs, and config options.
